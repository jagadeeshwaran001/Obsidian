This is used to look at the revision history.

We can find who written the specific line of code.

`-L {starting line numner}, {ending line number}` [[flag]] is used to view the changes of the specific lines.