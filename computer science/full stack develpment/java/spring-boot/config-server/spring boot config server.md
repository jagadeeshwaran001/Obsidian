1. first we need to add the `config server` dependency form [[spring-initilizer]].
2. Then we need to annotate [[configuration-annotation]] [[@EnableConfigServer]] in spring main application class.
3. 