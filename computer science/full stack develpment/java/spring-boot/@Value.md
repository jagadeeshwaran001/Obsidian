This [[spring-annotation]] used to inject the variable to the application from the [[application.properties or application.yml]] file or System Environmental variable.

Syntax

```java
@Value("${variable.name}")
Stirng variableName;
```
