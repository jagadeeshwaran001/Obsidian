[[DOM]] element in a js code is known as [[document in js]].

we can access the [[DOM]] by using the document [[Keyword]].

document [[Keyword]] return the The entire webpage in the browser's memory, as a JavaScript object..